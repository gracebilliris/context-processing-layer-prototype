#!/usr/bin/env python3
"""E2 (planned): noisy / larger corpora.

Extends ``data/generate_synthetic_telemetry.py`` with malformed payload
injection, duplicate and out-of-order events, partial failures, and two
additional domains beyond the running example. Intentionally a stub in
this release.
"""
raise SystemExit("E2 noisy-corpora extender is planned but not yet implemented.")
